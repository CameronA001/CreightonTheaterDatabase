spring.application.name=theater_database

# ===============================
# Supabase PostgreSQL
# ===============================
spring.datasource.url=jdbc:postgresql://aws-1-us-east-1.pooler.supabase.com:5432/postgres?sslmode=require
spring.datasource.username=postgres.touuhtqyyxfdkjntktlh
spring.datasource.password=IloveTheater123!
spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# HikariCP (safe defaults)
# ===============================
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=1800000

# ===============================
# JdbcTemplate
# ===============================
spring.jdbc.template.fetch-size=100

# ===============================
# OPTIONAL: schema.sql (use carefully)
# ===============================
# Uncomment ONLY if you explicitly want Spring to create tables
# spring.sql.init.mode=always
# spring.sql.init.schema-locations=classpath:schema.sql
# spring.sql.init.continue-on-error=true
